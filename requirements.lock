# Pinned non-torch dependencies. Torch / torchaudio are pinned in the Dockerfile
# because they need the CUDA-matching index URL.
#
# Regenerate with:
#   pip-compile --output-file=requirements.lock pyproject.toml
# or just bump versions here manually and verify tests still pass.

pytorch-lightning==2.4.0
miditoolkit==1.0.1
pretty_midi==0.2.10
numpy==1.26.4
pyyaml==6.0.2
tqdm==4.66.5
loguru==0.7.2
python-dotenv==1.0.1

# Optional but lightweight; kept in the base image so notebooks don't have to.
wandb==0.17.7
